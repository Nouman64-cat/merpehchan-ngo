# MongoDB
MONGODB_URL=mongodb+srv://user:password@cluster.mongodb.net
MONGODB_DB_NAME=meripehchan

# Auth (run `python -m scripts.create_admin` to generate these)
JWT_SECRET_KEY=
ADMIN_USERNAME=
ADMIN_PASSWORD_HASH=

# AWS S3 (team member photos)
AWS_IAM_KEY=
AWS_IAM_SECRET=
AWS_S3_BUCKET_NAME=
AWS_REGION=us-east-1

# AWS SES (reserved for future email notifications, not wired up yet)
AWS_SES_USERNAME=
AWS_SES_PASSWORD=
AWS_SES_FROM_EMAIL=

# Comma-separated list of allowed frontend origins
CORS_ORIGINS=http://localhost:3002
